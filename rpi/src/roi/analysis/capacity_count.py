"""
This script will likely be used if i wanna include min bounding box or confidence threshold
"""

def capacity_count(boxes, scores, class_ids, class_names):
    (class_ids == 1)
    
    
    # for box, score, c_id, c_name in zip(boxes, scores, class_ids, class_names):
    #     if c_id == 1:
            